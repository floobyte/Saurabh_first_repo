// Reading input
const one = parseInt(prompt("Enter the first number:"));
const two = parseInt(prompt("Enter the second number:"));
const three = parseInt(prompt("Enter the third number:"));
const four = parseInt(prompt("Enter the fourth number:"));
const five = parseInt(prompt("Enter the fifth number:"));
const six = parseInt(prompt("Enter the sixth number:"));
const seven = parseInt(prompt("Enter the seven number:"));
const eight = parseInt(prompt("Enter the eighth number:"));

// Calculating the product
const product = one * two * three * four * five * six * seven * eight;

// Printing the product
console.log(product);
